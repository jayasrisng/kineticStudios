# Kinetic Studios assets

This directory is reserved for product-owned Unity assets. Generated content and third-party packages must not be placed here.

The current structure contains only the Phase 1 studio shell: bootstrap loading, camera navigation, presentation assets, scenes, and UI. Feature-oriented directories should be introduced only when their owning systems are implemented.
